# @atfield/core

AT Protocol core primitives for ATField.

Current scope:

- transport and schema decoding primitives
- identity and DID-first types
- endpoint factories for XRPC or HTTP-backed integrations

This package is intentionally framework-agnostic.
