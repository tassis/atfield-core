{
	"name": "atfield-core-consumer-fixture",
	"private": true,
	"type": "module",
	"scripts": {
		"check": "tsc --noEmit -p tsconfig.json",
		"build": "tsc -p tsconfig.json",
		"smoke": "bun run ./dist/index.js"
	},
	"devDependencies": {
		"bun-types": "^1.3.14",
		"typescript": "^6.0.2"
	}
}
